Kept as close to the template as possible.

But I have added some small bits of code to show the device's current location rather than just the friends locations.

This is done by adding the MyLocationOverlay overlay object to the view map. That object takes care of updating the location and drawing the marker.